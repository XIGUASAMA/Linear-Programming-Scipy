1.“调包线性规划.ipynb”在Anaconda上编写，需相同软件上运行。同名.py文件可以直接在Python3 IDE上运行。
2.功能：输入可以为不等式形式，目标函数可以最小或最大；可以准确判断无界解，无可行解的情况。输入变量可以为自由变量，可以准确求出全局最优解。
3.不足：不能分别无穷多解和唯一解。
4.测试样例见根目录同名文件，需要将其改名为“input.txt”并放置在与源代码相同的目录内，输出见“output.txt”。
5.程序输入必须同测试样例的形式，否则会出现不可预知的错误。